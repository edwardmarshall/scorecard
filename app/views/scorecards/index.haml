%nav.card-index
  %a.none{:href => new_scorecard_url}
    .new-card
      +
  %span.card-index= link_to 'New Scorecard', new_scorecard_url

%section.cards.center
  - @scoreable_animals.each do |animal|
    = render :partial => 'card', :locals => { :animal => animal }
